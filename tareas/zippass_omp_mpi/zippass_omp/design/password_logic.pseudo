procedure descipherPassword(file, alphabet, maxLength) {
    // Will use test counters to interpret 
}

procedure generateNextPassword(testCounters, maxLength, currentLength, alphabetSize) {
    // Generates next testCounter combination
    return testCountersFlags
}

procedure translateCounterToPassword(testCounters) {

    return password
}